/*$(document).ready(function () {
    $('#editTaskButton').on('click', function () {
        var id = $(this).data('id');
        var title = $(this).data('title');
        var details = $(this).data('details');
        var devId = $(this).data('devId');
        var devName = $(this).data('devName');

        // Set values in the modal fields
        console.log(title, id, details);
        $('.taskTitleInput').val(title);
        $('.taskIdInput').val(id);
        $('.devId').val(devId);
        $('.devId').text(devName);

        // Set the selected value in the dropdown
      *//*  $('#developerSelect option').prop('selected', false); // Deselect all options
        $('#developerSelect option[value="' + devId + '"]').prop('selected', true);*//* // Select the desired option
        $('.taskDetailsInput').val(details);
    });
});*/
